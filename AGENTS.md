# Agent notes

**Read `CLAUDE.md` first** — repo map, commands, conventions (applies to all AI agents, not just Claude). API contract: `docs/API.md`.

## Expo HAS CHANGED
Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any frontend code.
